#!/bin/sh -e

# detect multiarch name
DEB_HOST_MULTIARCH=$(dpkg-architecture -qDEB_HOST_MULTIARCH)

# remove alternative
if [ "x$1" = "xremove" ]; then
	update-alternatives --remove gst1.0-imx /usr/lib/$DEB_HOST_MULTIARCH/gstreamer1.0-imx-x11
fi

#DEBHELPER#

exit 0
